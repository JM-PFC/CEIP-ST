{% extends 'BackendBundle:Default:index.html.twig' %}

{% block head %}{% endblock %}
{% block stylesheets %}{% endblock %}

{% block javascript %}

    <script>


    if($("#tabs div[aria-hidden='false'] .registro_equipamientos").attr("id")=="registro_equipamientos"){
        $("#equipamiento_nueva #equipamiento_nombre").attr("placeholder","Inserte el nombre para añadir nuevo equipamiento");
        $("#equipamiento_nueva #equipamiento_unidades").attr("placeholder","u."); 

        $('#registro_equipamientos .contenedor_registro_instalaciones input').focus(function(){
        $(this).data('placeholder',$(this).attr('placeholder')).attr('placeholder','');
        }).blur(function(){
            if($(this).attr("id")=="equipamiento_unidades"){
                $(this).attr('placeholder',"u.");
            }
            else{
                $(this).attr('placeholder',$(this).data('placeholder'));
            }
        });
    }
    else{
        /*

        $("#equipamiento_nueva").attr("id","instalación_nueva");
        $("#instalación_nueva #equipamiento_nombre").attr("placeholder","Inserte el nombre para añadir nueva instalación");
                */
        // Tabla de instalaciones
        $("#tabs div[aria-hidden='false'] .instalaciones #equipamiento_nueva").attr("id","instalación_nueva");
        $(".instalaciones #instalación_nueva #equipamiento_nombre").attr("id","instalación_nombre");
        $(".instalaciones #instalación_nueva #equipamiento_unidades").attr("id","instalación_unidades");
        $(".instalaciones #instalación_nueva #equipamiento_tipo").attr("id","instalación_tipo");
        $(".instalaciones #instalación_nueva #instalación_nombre").attr("placeholder","Inserte una nueva instalación");
        $(".instalaciones #instalación_nueva #instalación_unidades").val(1);
        $(".instalaciones #instalación_nueva #instalación_tipo").val("Instalación");
        $(".instalaciones #instalación_unidades").addClass("oculto");

        // Tabla de aulas
        $("#tabs div[aria-hidden='false'] .aulas #equipamiento_nueva").attr("id","aula_nueva");
        $(".aulas #aula_nueva #equipamiento_nombre").attr("id","aula_nombre");
        $(".aulas #aula_nueva #equipamiento_unidades").attr("id","aula_unidades");
        $(".aulas #aula_nueva #equipamiento_tipo").attr("id","aula_tipo");
        $(".aulas #aula_nueva #aula_nombre").attr("placeholder","Inserte una nueva aula");
        $(".aulas #aula_nueva #aula_unidades").val(1);
        $(".aulas #aula_nueva #aula_tipo").val("Aula");
        $(".aulas #aula_unidades").addClass("oculto");

        $('#registro_instalaciones .contenedor_registro_instalaciones input').focus(function(){
            $(this).data('placeholder',$(this).attr('placeholder')).attr('placeholder','');
            }).blur(function(){
            $(this).attr('placeholder',$(this).data('placeholder'));
        });
    }

    </script>

{% endblock %}

{% block body -%}
    {{form_start(form, {'action':path('equipamiento_create'),'method':'POST','attr':{'id':'equipamiento_nueva' ,'class':'equipamiento_form' , novalidate:'novalidate', 'autocomplete':'off'}})}}
        {{ form_errors(form) }}

		{{ form_widget(form.nombre) }}
		{{ form_widget(form.unidades) }}
    		 <div class="oculto">
        		{{ form_rest(form) }} 
    		</div>
    {{form_end(form)}}
{% endblock %}

