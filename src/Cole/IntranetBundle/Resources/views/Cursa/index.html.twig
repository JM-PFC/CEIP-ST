{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cursa list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Trimestre</th>
                <th>Nota</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cursa_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.trimestre }}</td>
                <td>{{ entity.nota }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cursa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cursa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cursa_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
